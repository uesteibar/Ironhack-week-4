<div class="row">
  <div class="jumbotron col-md-8 col-md-offset-2">
    <h1>
      <%= @concert.band %>
    </h1>
    <p>
      <%= @concert.date.strftime("%d.%m.%y") %>
    </p>
    <p>
      <%= "#{@concert.price} €" %>
    </p>
    <p>
      <%= "#{@concert.venue} - #{@concert.city}" %>
    </p>
    <p>
      <%= @concert.description %>
    </p>
  </div>
</div>
